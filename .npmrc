# Reproducibility and supply-chain posture.
engine-strict=true
# Do not run install scripts from transitive deps by default; allowlist explicitly.
# (pnpm 10 defaults to blocking, this makes the intent explicit and audited.)
strict-peer-dependencies=false
resolution-mode=highest
# Fail installs when the lockfile would change in CI.
prefer-frozen-lockfile=true
